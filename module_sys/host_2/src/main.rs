use rand::Rng;
use std::collections::HashMap;
// use std::cmp::Ordering;

// use std::io::Write;
// use std::io;

use std::io::{self, Write};

//谨慎使用全部引用
//通配符引用一般用在测试时常用。方便测试不同的测试用例
use std::collections::*;


// use std::{cmp::Ordering, io};    //普通写法
// 可使用嵌套路径在同一行完成库的导入

//有些库因为非常常用，编译器🧬器已经帮我们做了预导入处理
fn main() {
    println!("Hello, world!");
}
